#!/usr/bin/ksh

prog="/usr/bin/X11/vncpasswd"
mypass="<%= node[:ebs][:vncpw] %>"

cd "/"

/usr/bin/expect <<EOF
spawn "$prog"
expect "Password:"
send "$mypass\r"
expect "Verify:"
send "$mypass\r"
expect eof
exit
EOF
